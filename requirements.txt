# 核心依赖（CPU 和 GPU 通用）
torch>=1.11.0
torchvision>=0.12.0
Pillow>=9.0.0
numpy>=1.21.0
pandas>=1.3.0
matplotlib>=3.4.0
scikit-learn>=1.0.0   # 混淆矩阵 & 分类报告（可选，缺失时跳过）

# PyQt5 GUI
PyQt5>=5.15.0

# CPU 推理加速（可选，无此包则退回 PyTorch CPU）
onnx>=1.12.0
onnxruntime>=1.12.0

# 安装示例（纯 CPU 机台）:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
#   pip install Pillow numpy pandas matplotlib scikit-learn PyQt5 onnx onnxruntime
#
# 安装示例（GPU CUDA 11.8 机台）:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
#   pip install Pillow numpy pandas matplotlib scikit-learn PyQt5 onnx onnxruntime
