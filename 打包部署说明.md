# 钻石缺陷图像分类系统 — 打包与机台部署

在 **开发机** 将应用打成 Windows exe，部署到 **产线机台**。方案为 **ONNX 精简包**（不含 PyTorch，内置 CUDA 12 运行时与 cuDNN）。

---

## 1. 两种运行模式

| 模式 | 入口 | 推理 | 用途 |
|------|------|------|------|
| 开发版 | `python app.py` | PyTorch GPU + ONNX 回退；可选 SAHI/YOLO 大图检测 | 训练、调试、再训练、钻石检测分类 |
| 机台版 | `缺陷分类系统.exe` / `python app_deploy.py` | ONNX Runtime（GPU/CPU） | 产线单图/批量缺陷分类 |

机台版窗口标题显示「**· 机台版**」；不含 PyTorch / ultralytics，**无法在机台训练**（再训练页只读），**不含「钻石检测分类」**（SAHI 切片推理仅开发版 `app.py` 提供）。

`app_deploy.py` 设置 `DEFECTS_DEPLOY=1` 后复用 `app.py` 全部 UI，仅切换为 `inference_engine_onnx.InferenceEngine`。

---

## 2. 环境与依赖

### 2.1 两个 Conda 环境（推荐）

| 环境 | 用途 | 依赖文件 |
|------|------|----------|
| `cv-yolo`（或自建） | 训练、`python app.py` | `requirements.txt` |
| **`defects-deploy`** | **仅打包** | `requirements-deploy.txt` |

一键创建打包环境：

```powershell
powershell -ExecutionPolicy Bypass -File scripts/setup_deploy_env.ps1
```

手动创建：

```powershell
conda create -n defects-deploy python=3.12 -y
conda activate defects-deploy
pip install -r requirements-deploy.txt
```

`requirements-deploy.txt` 要点：

- `onnxruntime-gpu==1.20.1`（**勿用 1.26+**，PyInstaller 下易 DLL 初始化失败）
- `nvidia-cudnn-cu12`、`nvidia-cuda-runtime-cu12`、`nvidia-cublas-cu12`（打包时打入 `cuda_deps/`，机台无需单独装 cuDNN）
- `PyQt5`、`numpy`、`Pillow`、`pyinstaller`

> CPU 版 `onnxruntime` 与 `onnxruntime-gpu` 不可共存；`build_deploy.py` 会自动安装目标版本并卸载另一版本。

### 2.2 训练产物

打包前在 **开发环境** 完成训练：

```powershell
conda activate cv-yolo
python train.py --data_dir data --img_size 128
# 或仅补导出
python train.py --postprocess_only
```

`checkpoints/` 必需文件：

| 文件 | 说明 |
|------|------|
| `model.onnx` | ONNX 主文件 |
| `model.onnx.data` | 外部权重（EfficientNet 导出通常需要） |
| `class_map.json` | 五类缺陷名称 |
| `train_config.json` | **img_size**（推理尺寸，当前 128） |
| `class_thresholds.json` | 逐类阈值（推荐） |

`best_model.pt` 仅开发机需要，**不会**打入机台包。

### 2.3 打包前验收

```powershell
conda activate defects-deploy
python scripts/verify_deploy.py      # 有 GPU 时应显示 device: cuda
python app_deploy.py                 # 可选：GUI 本地调试机台模式
```

---

## 3. 打包

### 3.1 命令

```powershell
conda activate defects-deploy
cd <项目根目录>

python scripts/build_deploy.py              # 正式包（无控制台）
python scripts/build_deploy.py --console    # 调试闪退时用
python scripts/build_deploy.py --cpu-only   # 纯 CPU 机台（体积更小）
```

或使用批处理：

```powershell
scripts\build_deploy.bat
scripts\build_deploy.bat console
scripts\build_deploy.bat cpu-only
```

> **请勿**手写或恢复根目录 `.spec` 打包：缺少 staging / CUDA 收集 / 补丁步骤。正式打包一律走 `scripts/build_deploy.py`。

### 3.2 `build_deploy.py` 做了什么

1. 检测当前环境是否误装 PyTorch（有则警告）
2. 校验 `checkpoints/model.onnx` 存在
3. 安装/切换 `onnxruntime-gpu==1.20.1`（或 `--cpu-only` 时 CPU 版）并卸载冲突包
4. GPU 模式下安装 nvidia CUDA pip 包，收集 DLL 到 `build_staging/cuda_deps/`
5. 复制 checkpoints 必需文件到 `build_staging/checkpoints/`
6. 调用 PyInstaller 打包 `app_deploy.py`，注入 `pyinstaller_hooks/rthook_ort_dll.py`
7. 排除 torch、matplotlib、pandas、sklearn、nvidia Python 包等
8. 打包后 `_patch_dist`：补全 ORT Python 文件、去重 DLL、移除 `_internal/checkpoints/` 与 TensorRT Provider
9. 将 checkpoints 复制到 exe **同级** `checkpoints/`
10. 打印体积构成（实际 MB 数以本次打包输出为准）

临时目录：`build_staging/`（staging）、`build/`（PyInstaller 工作目录）；每次打包前会清空重建 `build_staging/`。

### 3.3 输出结构

```
dist/缺陷分类系统/
├── 缺陷分类系统.exe
├── checkpoints/           ← 与 exe 同级，更新模型只改这里
│   ├── model.onnx
│   ├── model.onnx.data
│   ├── class_map.json
│   ├── train_config.json
│   └── class_thresholds.json
└── _internal/
    ├── cuda_deps/         ← GPU 包：cuDNN / cuBLAS / cudart 等
    ├── onnxruntime/capi/  ← ORT 与 Provider
    └── PyQt5/ ...
```

**必须复制整个文件夹**，不能只拷 exe。

| 包类型 | 约体积（以 build_deploy.py 打印为准） |
|--------|--------------------------------------|
| GPU 包 | **~2.8 GB** |
| CPU 包 (`--cpu-only`) | **~1.5 GB** |

### 3.4 打包后验收

```powershell
python scripts/verify_frozen_sim.py
```

脚本在 `dist/缺陷分类系统/` 下启动 exe，并设置 `DEFECTS_VERIFY=1`（不启 GUI，仅测 ORT + 模型加载）。

或手动：

```powershell
cd dist\缺陷分类系统
set DEFECTS_VERIFY=1
缺陷分类系统.exe
```

| 退出码 | 含义 |
|--------|------|
| 0 | ORT 加载成功且 `device: cuda` |
| 1 | ORT 导入或模型加载失败 |
| 2 | ORT 正常但回退到 CPU |

手动 GUI 验收：双击 exe → 设置页「模型加载成功 [ONNX / GPU]」→ 批量检测正常。

---

## 4. 机台部署

### 4.1 目录

```
产线机/缺陷分类系统/
├── 缺陷分类系统.exe
├── checkpoints/
├── _internal/
├── app_config.json      ← 首次运行生成（GPU 开关、模型路径等）
└── corrections/         ← 误分类归档（运行时创建）
```

### 4.2 GPU

- 内置 cuDNN 9 / CUDA 12 runtime，**无需**机台单独安装 cuDNN
- 需要 NVIDIA 显卡 + 驱动
- 设置页勾选「使用 GPU」；失败则自动 CPU
- 若机台曾设置 `CUDA_PATH` / `CUDA_HOME` 指向旧版 CUDA，建议删除或留空（runtime hook 会尝试清除）

### 4.3 仅更新模型

开发机重训后，覆盖机台 `checkpoints/`：

`model.onnx`、`model.onnx.data`、`class_map.json`、`train_config.json`、`class_thresholds.json`

无需重打 exe（除非升级程序版本或 ORT/CUDA 依赖变更）。

### 4.4 误分类回收

机台 `corrections/`（按类别分子文件夹）→ 拷回开发机 →  
`python train.py --finetune --extra_data_dirs corrections` → 再更新机台 `checkpoints/`。

---

## 5. 推理源码架构

机台 exe **不含 PyTorch**；推理由 ONNX 引擎 + 公共模块组成：

```
app_deploy.py  →  app.py（DEFECTS_DEPLOY=1）
                      └─ inference_engine_onnx.py
                             └─ inference_common.py   ← 开发/机台共用
```

| 文件 | 作用 |
|------|------|
| `app_deploy.py` | 打包入口；`DEFECTS_VERIFY=1` 无 GUI 验收 |
| `inference_engine_onnx.py` | 机台推理：ORT Session、NumPy 预处理、`predict` / `predict_batch` |
| `inference_common.py` | `run_batch_predict`；`logits_row_to_result`；元数据读取 |
| `inference_engine.py` | 仅开发机 `python app.py`，**不打入 exe** |
| `app_paths.py` | exe 路径；`setup_ort_dll_paths()`（含 preload，业务侧统一入口） |
| `pyinstaller_hooks/rthook_ort_dll.py` | 冻结进程最早配置 DLL（与 `app_paths` 语义一致、故意独立） |
| `scripts/build_deploy.py` | **唯一**正式打包入口 |
| `scripts/setup_deploy_env.ps1` | 创建 defects-deploy 环境 |
| `scripts/verify_deploy.py` | 打包前源码验收 |
| `scripts/verify_frozen_sim.py` | 打包后 exe 验收 |

> Windows DLL：**rthook**（最早）+ **`setup_ort_dll_paths`**（业务 import ORT 前）。修改推理行为（批量逻辑、置信度、阈值）时改 `inference_common.py` 即可同时影响开发 ONNX 回退与机台版。

---

## 6. 故障排查

### 6.1 onnxruntime 加载失败 / DLL 初始化失败

1. 用当前 `build_deploy.py` 重打（ORT **1.20.1**）
2. 确认复制了完整 `_internal/`（含 `cuda_deps/`、`onnxruntime/capi/`）
3. 删除机台 `CUDA_PATH`、`CUDA_HOME`
4. 杀毒软件加入白名单

### 6.2 检测报尺寸错误（224 vs 128）

`train_config.json` 中 `img_size` 须与训练一致；覆盖机台 `checkpoints/` 后在设置页重新加载。

### 6.3 闪退

`python scripts/build_deploy.py --console`，在 cmd 中运行 exe 查看 stderr。

### 6.4 GPU 变 CPU

检查 `_internal/cuda_deps/` 是否完整、显卡驱动是否可用；用 `--console` 查看 ORT Provider 日志；运行 `verify_frozen_sim.py` 看退出码。

### 6.5 体积

GPU 包约 2.8 GB 主要为 cuDNN/cuBLAS/ORT CUDA，属正常。无 GPU 机台用 `--cpu-only`。  
Python 源码（含 `inference_common.py`）体积极小，优化推理逻辑**不会**明显增大打包体积。

### 6.6 单张与批量结果不一致

若使用旧版 exe：批量路径曾存在双重 softmax，导致置信度与单张不同。  
**解决**：用当前代码重打 exe（内含 `run_batch_predict` 统一逻辑）。  
若类别相同但置信度与得分条第一名不同，多为**逐类阈值决策**所致，见 [README.md](README.md)「置信度含义」。

### 6.7 打包环境检测到 PyTorch

`build_deploy.py` 会警告。请 `conda activate defects-deploy`，避免把 PyTorch 打入 exe 导致体积暴增。

---

## 7. 命令速查

```powershell
# 环境
powershell -ExecutionPolicy Bypass -File scripts/setup_deploy_env.ps1
conda activate defects-deploy

# 打包
python scripts/build_deploy.py
python scripts/build_deploy.py --console
python scripts/build_deploy.py --cpu-only

# 验收
python scripts/verify_deploy.py
python scripts/verify_frozen_sim.py

# 训练（开发环境 cv-yolo）
python train.py --data_dir data --img_size 128
python train.py --postprocess_only
```

---

## 8. 相关文档

- [README.md](README.md) — 项目总览与训练
- [QT应用开发说明.md](QT应用开发说明.md) — 修改界面时阅读
