# 钻石缺陷图像分类系统 — 打包与机台部署

在 **开发机** 将应用打成 Windows exe，整包复制到 **产线机台**。方案为 **ONNX 分类 + SAHI/YOLO 大图检测**（机台均有 GPU）。

现行正式打包以开发环境 **`cv-yolo`** 为准：不降级已安装的 ORT / CUDA pip 包，DLL 以该环境能无障碍运行的组合为准。

---

## 1. 两种运行模式

| 模式 | 入口 | 分类 | 检测 | 用途 |
|------|------|------|------|------|
| 开发版 | `python app.py` | PyTorch GPU，无 GPU 时 ONNX CPU 回退 | SAHI/YOLO | 训练、调试、再训练 |
| 机台版 | `缺陷分类系统.exe` / `python app_deploy.py` | ONNX Runtime GPU | SAHI/YOLO | 产线大图检测分类 + 单图/批量分类 |

机台版窗口标题显示「**· 机台版**」；再训练页只读。「钻石检测分类」与开发版同一页，分类引擎为 ONNX。

`app_deploy.py` 设置 `DEFECTS_DEPLOY=1` 后复用 `app.py` 全部 UI，分类切换为 `inference_engine_onnx.InferenceEngine`。冻结 exe 同样走该路径（`is_frozen()`）。

启动时 `MainWindow._auto_load_model` 自动加载 `app_config.json` 中的 `onnx_path`（机台默认同级 `checkpoints/model.onnx`）。设置页默认管理员锁定，日常检测不必进设置。

---

## 2. 环境与依赖

### 2.1 当前实际用法

| 环境 | 用途 |
|------|------|
| **`cv-yolo`** | 训练、`python app.py`、**正式机台打包** |
| `defects-deploy` | 可选隔离环境（`scripts/setup_deploy_env.ps1`），非当前正式路径 |

`cv-yolo` 侧已验证可打包的关键版本（以本机当时安装为准，脚本**不强制改写**）：

- Python 3.12、torch 2.12.0+cu132、onnxruntime-gpu（开发机曾为 1.26.0）
- ultralytics、opencv、matplotlib、sympy、PyQt5、PyInstaller

### 2.2 `requirements-deploy.txt`

隔离环境清单仍保留：ORT 回退版本写的是 `onnxruntime-gpu==1.20.1`。  
**`build_deploy.py` 行为**：环境里已能 `import onnxruntime` 则**保留现有版本**；仅未安装时才按该版本安装。不要在 `cv-yolo` 里强制降级。

YOLO 还需要 CUDA 版 `torch` / `torchvision`（按本机 CUDA 安装，不要写死 cu118）。

### 2.3 训练产物（分类）

打包前在 **cv-yolo** 完成训练或确认 `checkpoints/` 齐全：

| 文件 | 说明 |
|------|------|
| `model.onnx` | ONNX 主文件 |
| `model.onnx.data` | 外部权重 |
| `class_map.json` | 五类缺陷名称 |
| `train_config.json` | **img_size**（当前 128） |
| `class_thresholds.json` | 逐类阈值 |

`checkpoints/best_model.pt` 仅开发机分类用，**不打入**机台包。  
YOLO 检测权重由脚本从 `app_config.json` 的 `yolo_path`（或 `detect_weights/best.pt`）复制到 exe 同级 `detect_weights/best.pt`。**不要**把开发机里带绝对路径的 `app_config.json` 拷到机台。

### 2.4 打包前验收

```powershell
conda activate cv-yolo
python scripts/verify_deploy.py      # 有 GPU 时应显示 device: cuda
python app_deploy.py                 # 可选：GUI 本地调试机台模式
```

---

## 3. 打包

### 3.1 命令

```powershell
conda activate cv-yolo
cd <项目根目录>

python scripts/build_deploy.py              # 正式包（无命令行窗口，--windowed）
python scripts/build_deploy.py --console    # 仅排障：保留控制台看 traceback
python scripts/build_deploy.py --cpu-only   # 纯 CPU（本项目机台均有 GPU，一般不用）
```

或：`scripts\build_deploy.bat` / `scripts\build_deploy.bat console`

> **请勿**用根目录 `缺陷分类系统.spec` 作为正式入口：该文件由 PyInstaller 每次覆盖生成，缺少 staging / 补丁 / 写配置等步骤。一律走 `scripts/build_deploy.py`。

打包时请先**关闭正在运行的 exe**，否则会因目录占用失败（WinError 5 / 32）。

### 3.2 `build_deploy.py` 实际步骤

1. 校验 torch / ultralytics / opencv（缺则退出）
2. 校验 `checkpoints/model.onnx`
3. 确认 ORT 可导入（已装则不降级）；GPU 模式检查 cudart/cublas，收集 **非 cudnn** 的 nvidia pip DLL 到 `build_staging/cuda_deps/`（cudnn 用 `torch/lib`，避免 CUDA 12/13 混用）
4. 复制分类 checkpoints 到 staging
5. PyInstaller `--onedir` 打包 `app_deploy.py`，注入 `rthook_ort_dll.py`
6. `--collect-all`：`ultralytics`、`torch`、`torchvision`、`cv2`、`matplotlib`、`sympy`
7. 排除：`torchaudio`、`pandas`、`sklearn`、`tensorboard`、`onnx`、`onnxscript` 等（**不排除 sympy / matplotlib**）
8. `_patch_dist`：补全 ORT Python 文件、去重 DLL、去掉 TensorRT Provider
9. 将 checkpoints 放到 exe **同级**；YOLO 复制到 `detect_weights/best.pt`
10. 写入/覆盖机台版 `app_config.json`（相对路径，长宽比默认 **1.5**）

日志里大量 `ERROR: Hidden import 'torch.distributed...' not found` 来自 `--collect-all torch`，**不影响** YOLO 推理，可忽略。

临时目录：`build_staging/`、`build/`；每次打包会重建 `build_staging/`。

### 3.3 输出结构

```
dist/缺陷分类系统/
├── 缺陷分类系统.exe
├── app_config.json        ← 机台相对路径配置（打包生成）
├── checkpoints/           ← 分类 ONNX，热更新只改这里
│   ├── model.onnx
│   ├── model.onnx.data
│   ├── class_map.json
│   ├── train_config.json
│   └── class_thresholds.json
├── detect_weights/
│   └── best.pt            ← YOLO 检测权重
└── _internal/
    ├── cuda_deps/
    ├── onnxruntime/capi/
    ├── torch/
    ├── sympy/
    └── PyQt5/ ...
```

**必须复制整个文件夹**。正式 GPU 包体积约 **6 GB**（torch + cuda_deps + YOLO 权重为主）。

### 3.4 打包后验收

```powershell
python scripts/verify_frozen_sim.py
```

对 exe 设置 `DEFECTS_VERIFY=1`（不启 GUI）。模型加载成功即返回 `0`；若 GPU 未启用，会打印警告但不视为失败。

手动 GUI：双击 exe → **无命令行窗口** → 状态栏「模型加载成功 [ONNX / GPU]」→「钻石检测分类」可直接开始处理。

---

## 4. 机台部署

### 4.1 目录

```
产线机/缺陷分类系统/
├── 缺陷分类系统.exe
├── app_config.json
├── checkpoints/
├── detect_weights/
├── _internal/
├── corrections/         ← 运行时创建
└── sahi_output/         ← 检测输出（运行时创建）
```

### 4.2 GPU / DLL

- 机台需要 NVIDIA 显卡与驱动；**不必**再装一套 CUDA Toolkit / cuDNN
- 冻结进程：`rthook_ort_dll.py` 最早配置 DLL 搜索路径，并清掉 `CUDA_PATH` / `CUDA_HOME`，避免机台旧 Toolkit 抢加载
- 业务侧 `app_paths.setup_ort_dll_paths()`：`torch/lib` 优先于 ORT capi / `cuda_deps`，cudnn 不走 nvidia-cudnn-cu12
- 分类引擎采用“GPU 优先、CPU 保底”：若某台机子的 CUDA / cuDNN 初始化失败，程序会自动回退 CPU 继续可用
- 设置页「使用 GPU」默认勾选；回退 CPU 后仍可继续检测与分类

### 4.3 仅更新模型

覆盖机台 `checkpoints/`：`model.onnx`、`model.onnx.data`、`class_map.json`、`train_config.json`、`class_thresholds.json`，重启即可（启动自动加载）。

更新 YOLO：覆盖 `detect_weights/best.pt`。

无需重打 exe（除非改程序或依赖）。

### 4.4 误分类回收

机台 `corrections/` → 拷回开发机 →  
`python train.py --finetune --extra_data_dirs corrections` → 再更新机台 `checkpoints/`。

### 4.5 设置页

默认只读。改 YOLO 路径或 SAHI 参数需输入管理员密码（`app.py` 中 `ADMIN_PAGE_PASSWORD`）。日常检测用打包写入的默认配置即可。

---

## 5. 推理源码架构

```
app_deploy.py  →  app.py（DEFECTS_DEPLOY=1 或 frozen）
                      ├─ inference_engine_onnx.py   ← 缺陷分类
                      │      └─ inference_common.py
                      └─ sahi_detector.py           ← YOLO 大图检测
                             └─ ultralytics / torch
```

| 文件 | 作用 |
|------|------|
| `app_deploy.py` | 打包入口；`DEFECTS_VERIFY=1` 无 GUI 验收 |
| `inference_engine_onnx.py` | 机台分类：ORT Session、`predict` / `predict_batch` / `predict_batch_images` |
| `inference_common.py` | 批量循环、logits→结果、阈值 |
| `sahi_detector.py` | SAHI 切片 + 后处理（含长宽比过滤，默认 1.5）+ 调用分类引擎 |
| `inference_engine.py` | 仅 `python app.py` 分类（PyTorch 优先） |
| `app_paths.py` | 路径与 DLL |
| `pyinstaller_hooks/rthook_ort_dll.py` | 冻结进程最早配置 DLL |
| `scripts/build_deploy.py` | **唯一**正式打包入口 |

---

## 6. 故障排查

### 6.1 启动弹出命令行窗口

正式包必须**不加** `--console`。旧调试包需用 `python scripts/build_deploy.py` 重打。

### 6.2 提示未找到 `app_config.json` / 分类引擎未加载

确认 exe 同级有打包生成的 `app_config.json` 与 `checkpoints/model.onnx`。不要用开发机绝对路径配置覆盖机台文件。设置页保存需先解锁。

### 6.3 YOLO：`No module named 'sympy'` / `'matplotlib'`

旧包曾 `--exclude-module`。当前已 collect-all，需重打。

### 6.4 `CUDNN_STATUS_SUBLIBRARY_VERSION_MISMATCH` / WinError 127

cudnn 被 CUDA 12 pip 包与 torch cu132 混用。现行脚本跳过收集 nvidia cudnn，搜索路径以 `torch/lib` 为先。

### 6.5 打包 PermissionError / 拒绝访问 dist

先退出正在运行的「缺陷分类系统.exe」，再打包。

### 6.6 检测尺寸 224 vs 128

`train_config.json` 的 `img_size` 须为 128；覆盖 checkpoints 后重启。

### 6.7 GPU 变 CPU

查驱动、`_internal/cuda_deps/`、`verify_frozen_sim.py` 退出码。排障可临时 `--console`。

### 6.8 长宽比异常框仍留下

默认 `sahi_max_aspect_ratio=1.5`（`max(w,h)/min(w,h)` 大于该值剔除）。若机台 `app_config.json` 仍是 2.5，改配置后重启，或用当前脚本重打。

---

## 7. 命令速查

```powershell
conda activate cv-yolo
python scripts/verify_deploy.py
python scripts/build_deploy.py
python scripts/verify_frozen_sim.py

# 训练（同一环境）
python train.py --data_dir data --img_size 128
python train.py --postprocess_only
```

---

## 8. 相关文档

- [README.md](README.md) — 项目总览与训练
- [QT应用开发说明.md](QT应用开发说明.md) — 修改界面
- [废弃文档/00_归档说明.md](废弃文档/00_归档说明.md) — 过时过程文档
