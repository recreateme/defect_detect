# 钻石检测分类 — 后处理改进现状与后续建议

> 面向 `sahi_detector.py` 的 SAHI 切片检测 + 缺陷分类流水线。  
> **阶段一后处理已落地**；本文保留问题分析与未完成项，避免按「尚未实现」重复开发。

---

## 0. 当前检测流程

```
大图 → slice_positions 切片(默认 1280, overlap 0.20)
     → YOLO 批量推理 → 坐标映射回原图
     → iou_nms
     → suppress_contained_boxes（IoS 包含抑制）
     → filter_small_area（相对中位面积）
     → filter_aspect_ratio（可选长宽比）
     → filter_edge_boxes（边缘不完整目标）
     → crop_with_padding → InferenceEngine.predict_batch
     → 可视化 + 统计（含 contained/small/edge 等 skipped 计数）
```

相关符号：`SahiDetector.detect`、`SahiPipeline.process_image`；配置键见 `app.py` 中 `_DEFAULT_CFG_FULL`（`sahi_ios_thresh` 等），设置页「切片推理配置」可调。

---

## 1. 已完成（阶段一）

| 能力 | 实现 |
|------|------|
| IoS 包含抑制 | `suppress_contained_boxes` |
| 面积过滤 | `filter_small_area` |
| 长宽比过滤 | `filter_aspect_ratio`（配置 `sahi_max_aspect_ratio`） |
| 边缘剔除 | `filter_edge_boxes` |
| UI / 配置 | `_DEFAULT_CFG_*`、设置页控件、`SahiPipelineWorker` 透传 |
| 统计可追溯 | 流水线统计含各类 skipped 计数 |

默认初值（可在设置页或 `app_config.json` 调整）：

| 参数 | 初值 |
|------|------|
| `sahi_ios_thresh` | 0.65（≤0 关闭） |
| `sahi_min_area_ratio` | 0.45（≤0 关闭） |
| `sahi_max_aspect_ratio` | 2.5（≤0 关闭） |
| `sahi_edge_filter` | true |
| `sahi_edge_margin_px` | 20 |

分类侧置信度：**不使用**全局 UI 旋钮；以 `checkpoints/class_thresholds.json` 逐类阈值为准。

---

## 2. 问题回顾（已解决的根因）

**小面积误检 / 大框套小框**：标准 IoU-NMS 对「小框几乎落在大框内」不敏感；已用 IoS + 面积过滤弥补。

**四周不完整钻石误分类**：边缘半颗钻石属 OOD；已用边缘边距剔除，避免送入分类器污染统计。

---

## 3. 后续建议（未做）

### 可视化与可追溯
- 在 `visualization_classified.jpg` 中用不同样式标出被后处理剔除的框。
- `result.json` 记录每框 `kept` / `reason`。

### 参数标定
- 用代表性大图网格搜索 `ios_thresh`、`min_area_ratio`、`edge_margin_px`。

### 检测 / 分类模型
- 提高切片 `overlap`（0.25~0.30）、难负样本重训、class-agnostic NMS。
- 分类：OOD 拒识、裁剪 padding 与训练一致、继续 `corrections/` 主动学习闭环。

### 性能（可选）
- 分类前内存 crop 直接预处理，减少「写盘再读」往返（当前维护向整理未改热路径）。

---

## 4. 注意事项

- 后处理越激进，误检越少但漏检越多；阈值须在标注集上标定。
- SAHI 仅开发版；机台 ONNX 缺陷分类主链路不受影响。
- 修改批量/阈值逻辑优先改 `inference_common.py`。
